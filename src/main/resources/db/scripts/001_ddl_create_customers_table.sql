create table customers (
                        id serial primary key ,
                        name varchar(150),
                        address varchar(150),
                        email varchar(150)
);